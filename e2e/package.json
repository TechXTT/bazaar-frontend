{
  "name": "bazaar-e2e",
  "private": true,
  "description": "End-to-end order-lifecycle tests for The Bazaar (buyer/seller escrow flows).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "ethers": "^6.13.4"
  }
}
